#pragma once
#include <iostream>

const float cDegree = 459.67f;
const float cCoeff = 1.8f;

float kelvinToFahrenheit(float kelvin);
void printFahrenheitTemperature(const float kelvin, const float fahrenheit);
